<h1>Edit Artist</h1><br><br>
<%= form_for @artist do |f| %>

Edit Artist Name:
<%= f.text_field :name, placeholder: "#{@artist.name}" %><br><br>

Edit Artist Bio:
<%= f.text_field :bio, :size=>"100", placeholder: "#{@artist.bio}" %><br><br>
<%= f.submit "Update Artist" %>
<% end %>